# 📋 НАСТРОЙКА ПЕРЕМЕННЫХ ОКРУЖЕНИЯ (.env)

**Статус:** ✅ ПОЛНОЕ РУКОВОДСТВО  
**Дата:** 2025-12-08

---

## ⚡ БЫСТРЫЙ СТАРТ (5 МИНУТ)

### 1️⃣ Создать .env файл

```bash
cd ~/send-to-kindle-telegram-bot
cp .env.example .env
nano .env  # или vim, или вы редактор
```

### 2️⃣ Заполнить обязательные переменные

```bash
# Telegram бот токен (от @BotFather)
UBOT_TELEGRAM_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Email отправителя
UBOT_EMAIL_FROM=your-email@gmail.com

# Пароль приложения (НЕ главный пароль!)
UBOT_PASSWORD=xxxx-xxxx-xxxx-xxxx

# SMTP сервер
UBOT_SMTP_HOST=smtp.gmail.com
UBOT_SMTP_PORT=587

# Kindle email
UBOT_EMAIL_TO=your-kindle@kindle.com
```

### 3️⃣ Перезагрузить контейнер

```bash
docker compose down
docker compose up -d
docker compose logs sendtokindle --tail 20
```

**Готово! 🎉**

---

## 📖 ПОЛНОЕ РУКОВОДСТВО

### UBOT_TELEGRAM_TOKEN

**Что это?** Токен доступа к твоему Telegram боту

**Где взять?**
1. Откройте Telegram
2. Напишите @BotFather
3. Выберите /newbot
4. Следуйте инструкциям
5. Получите токен вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

**Пример:**
```bash
UBOT_TELEGRAM_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

**⚠️ ВАЖНО:** Это твой секретный ключ! Не делись им с другими!

---

### UBOT_EMAIL_FROM

**Что это?** Email адрес, с которого отправляются книги на Kindle

**Какой email использовать?**
- ✅ Gmail (рекомендуется)
- ✅ Яндекс.Почта
- ✅ Outlook / Microsoft 365
- ✅ Любой IMAP/SMTP email

**Пример:**
```bash
UBOT_EMAIL_FROM=your-email@gmail.com
```

**⚠️ ВАЖНО:** Этот email должен быть добавлен в Amazon белый лист!

Как добавить:
1. Перейти https://www.amazon.com/gp/digital/fiona/
2. Найти "Approved Personal Document E-mail List"
3. Добавить твой email
4. Убедиться что включено "Receive e-mail-based Kindle documents"

---

### UBOT_PASSWORD

**Что это?** Пароль приложения для твоего email

**⚠️ КРИТИЧЕСКИ ВАЖНО:** Это НЕ твой основной пароль почты!

**Для Gmail:**
1. Перейти https://myaccount.google.com/apppasswords
2. Выбрать "Mail" и "Windows Computer" (или твоё устройство)
3. Скопировать сгенерированный пароль
4. Это будет 16 символов (с пробелами)

**Для Яндекса:**
1. Перейти https://passport.yandex.ru/
2. В безопасности создать пароль приложения
3. Скопировать пароль

**Для Outlook:**
1. Перейти https://account.live.com/
2. Создать пароль приложения
3. Скопировать пароль

**Пример:**
```bash
UBOT_PASSWORD=xxxx-xxxx-xxxx-xxxx
```

**⚠️ ВАЖНО:** Без правильного пароля приложения нельзя отправлять письма!

---

### UBOT_SMTP_HOST

**Что это?** Адрес SMTP сервера почтовой службы

**Популярные значения:**

| Провайдер | SMTP Host | Порт | Тип |
|-----------|-----------|------|-----|
| Gmail | `smtp.gmail.com` | 587 | TLS |
| Яндекс | `smtp.yandex.com` | 587 | TLS |
| Яндекс (альт) | `smtp.yandex.ru` | 587 | TLS |
| Outlook | `smtp.live.com` | 587 | TLS |
| Outlook (альт) | `smtp-mail.outlook.com` | 587 | TLS |
| Microsoft 365 | `smtp.office365.com` | 587 | TLS |

**Пример для Gmail:**
```bash
UBOT_SMTP_HOST=smtp.gmail.com
```

**Если не знаешь:** Гугли "SMTP settings [твой email провайдер]"

---

### UBOT_SMTP_PORT

**Что это?** Порт SMTP сервера

**Стандартные значения:**
- `587` - TLS (рекомендуется, безопасно)
- `465` - SSL (альтернатива, тоже безопасно)
- `25` - Обычный (не рекомендуется, редко работает)

**Для большинства случаев:**
```bash
UBOT_SMTP_PORT=587
```

**Если порт не работает:** Попробуй другой порт из таблицы выше

---

### UBOT_EMAIL_TO (или UBOT_KINDLE_DEVICES)

**Что это?** Email адрес твоего Kindle

#### Вариант А: ОДНО устройство

```bash
UBOT_EMAIL_TO=your-kindle@kindle.com
```

**Как найти свой Kindle email?**
1. Перейти https://www.amazon.com/gp/digital/fiona/
2. В разделе "Devices" найти твоё устройство
3. Увидишь email вида `user123456@kindle.com`

**Пример:**
```bash
UBOT_EMAIL_TO=john_smith_123456@kindle.com
```

#### Вариант Б: НЕСКОЛЬКО устройств (НОВОЕ!)

```bash
UBOT_KINDLE_DEVICES=Device1:email1@kindle.com|Device2:email2@kindle.com
```

**Формат:**
```
ИмяУстройства:email@kindle.com|ВтороеУстройство:email2@kindle.com
```

**Примеры:**
```bash
# Семья
UBOT_KINDLE_DEVICES=My Paperwhite:john@kindle.com|Wife Oasis:jane@kindle.com|Kids Tablet:kids@kindle.com

# Рабочие + личные
UBOT_KINDLE_DEVICES=Work:work@kindle.com|Personal:personal@kindle.com

# Много устройств
UBOT_KINDLE_DEVICES=Bedroom:bed@kindle.com|Office:office@kindle.com|Travel:travel@kindle.com
```

**⚠️ ВАЖНО:** Если используешь `UBOT_KINDLE_DEVICES`, оставь `UBOT_EMAIL_TO` пустым!

---

### UBOT_SMTP_INSECURE (опционально)

**Что это?** Флаг для отключения проверки SSL сертификата

**Когда использовать?** Только если:
- Используешь самоподписанный сертификат
- SMTP сервер с проблемами SSL

**Значения:**
```bash
# Выключено (рекомендуется)
UBOT_SMTP_INSECURE=false
# или просто закомментируй эту строку

# Включено (только если нужно)
UBOT_SMTP_INSECURE=true
```

**По умолчанию:** false (безопасно)

---

## ✅ ПОЛНЫЙ ПРИМЕР .env

```bash
# ═══════════════════════════════════════════════════════════════════════
# TELEGRAM BOT CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════
UBOT_TELEGRAM_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# ═══════════════════════════════════════════════════════════════════════
# EMAIL CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════
UBOT_EMAIL_FROM=your-email@gmail.com
UBOT_PASSWORD=xxxx-xxxx-xxxx-xxxx

# ═══════════════════════════════════════════════════════════════════════
# KINDLE CONFIGURATION - Choose ONE option
# ═══════════════════════════════════════════════════════════════════════

# Option A: Single Kindle device
UBOT_EMAIL_TO=your-kindle@kindle.com

# Option B: Multiple Kindle devices (uncomment to use)
# UBOT_KINDLE_DEVICES=Device1:email1@kindle.com|Device2:email2@kindle.com

# ═══════════════════════════════════════════════════════════════════════
# SMTP CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════
UBOT_SMTP_HOST=smtp.gmail.com
UBOT_SMTP_PORT=587
UBOT_SMTP_INSECURE=false
```

---

## 🧪 ПРОВЕРКА КОНФИГУРАЦИИ

### 1️⃣ Проверить что .env существует

```bash
ls -la ~/send-to-kindle-telegram-bot/.env
```

**Должно быть:**
```
-rw-r--r-- 1 root root 500 Dec  8 10:00 .env
```

**Если нет файла:**
```bash
cp ~/.env.example ~/.env
nano ~/.env  # Отредактировать
```

### 2️⃣ Проверить что переменные заполнены

```bash
grep "^UBOT_" ~/.env
```

**Должны видеть:**
```
UBOT_TELEGRAM_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
UBOT_EMAIL_FROM=your-email@gmail.com
UBOT_PASSWORD=xxxx-xxxx-xxxx-xxxx
UBOT_SMTP_HOST=smtp.gmail.com
UBOT_SMTP_PORT=587
UBOT_EMAIL_TO=your-kindle@kindle.com
```

### 3️⃣ Проверить что нет пустых значений

```bash
grep "=$" ~/.env
```

**Не должно быть строк с пустыми значениями!**

### 4️⃣ Проверить в работающем контейнере

```bash
# Перезагрузить контейнер с новой конфигурацией
docker compose down
docker compose up -d
sleep 3

# Проверить логи
docker compose logs sendtokindle --tail 20
```

**Должны видеть нормальный запуск, БЕЗ ошибки:**
```
❌ could not start telegram bot: emailto not set
```

---

## 🆘 ЧАСТЫЕ ОШИБКИ И РЕШЕНИЯ

### Ошибка: "could not start telegram bot: emailto not set"

**Причина:** Не установлена переменная UBOT_EMAIL_TO или UBOT_KINDLE_DEVICES

**Решение:**
```bash
# Проверить .env
cat ~/.env | grep "EMAIL_TO\|KINDLE_DEVICES"

# Должно быть одно из:
# UBOT_EMAIL_TO=your-kindle@kindle.com
# ИЛИ
# UBOT_KINDLE_DEVICES=Device:email@kindle.com

# Если нет, добавить в .env и перезагрузить
nano ~/.env
docker compose down && docker compose up -d
```

### Ошибка: "could not start telegram bot: telegram token not set"

**Причина:** Не установлена переменная UBOT_TELEGRAM_TOKEN

**Решение:**
```bash
# Проверить есть ли токен
grep UBOT_TELEGRAM_TOKEN ~/.env

# Если пусто, получить новый от @BotFather и добавить
nano ~/.env
UBOT_TELEGRAM_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Перезагрузить
docker compose down && docker compose up -d
```

### Ошибка: "could not start telegram bot: email from not set"

**Причина:** Не установлена переменная UBOT_EMAIL_FROM

**Решение:**
```bash
grep UBOT_EMAIL_FROM ~/.env
# Добавить если нет
nano ~/.env
UBOT_EMAIL_FROM=your-email@gmail.com

docker compose down && docker compose up -d
```

### Ошибка: "Authentication failed" при отправке письма

**Причины:**
1. Неправильный пароль приложения
2. Email не добавлен в белый список Amazon
3. SMTP сервер требует TLS/SSL

**Решение:**
```bash
# 1. Проверить пароль
grep UBOT_PASSWORD ~/.env

# 2. Убедиться что это пароль ПРИЛОЖЕНИЯ, не главный пароль
# Для Gmail: https://myaccount.google.com/apppasswords

# 3. Проверить что email добавлен в Amazon
# https://www.amazon.com/gp/digital/fiona/
# "Approved Personal Document E-mail List"

# 4. Убедиться что SMTP порт правильный (587 для TLS)
grep UBOT_SMTP_PORT ~/.env
```

---

## 💾 РЕЗЕРВНАЯ КОПИЯ

После того как .env работает, сохрани его в безопасном месте:

```bash
# Создать backup
cp ~/.env ~/.env.backup

# Если что-то сломается, восстановить
cp ~/.env.backup ~/.env
```

---

## 🔒 БЕЗОПАСНОСТЬ

### ⚠️ НИКОГДА не делись следующим:
- `UBOT_TELEGRAM_TOKEN` - твой секретный ключ бота
- `UBOT_PASSWORD` - пароль приложения
- `UBOT_EMAIL_FROM` - если не хочешь, чтобы люди знали твой email

### ✅ БЕЗОПАСНО делиться:
- `.env.example` - это шаблон без значений
- Описание переменных и как их получить

### 🛡️ Как защитить .env?

```bash
# Установить права доступа (только владелец может читать)
chmod 600 ~/.env

# Проверить
ls -la ~/.env
# Должно быть: -rw------- 1 root root ...

# Добавить в .gitignore (если используешь Git)
echo ".env" >> ~/.gitignore
echo ".env.backup" >> ~/.gitignore
```

---

## ✨ ИТОГОВЫЙ ЧЕКЛИСТ

- [ ] Создан файл `.env`
- [ ] Получен токен от @BotFather
- [ ] Установлен UBOT_TELEGRAM_TOKEN
- [ ] Установлен UBOT_EMAIL_FROM
- [ ] Создан пароль приложения
- [ ] Установлен UBOT_PASSWORD
- [ ] Установлен UBOT_SMTP_HOST
- [ ] Установлен UBOT_SMTP_PORT
- [ ] Установлен UBOT_EMAIL_TO ИЛИ UBOT_KINDLE_DEVICES
- [ ] Email добавлен в Amazon белый список
- [ ] Контейнер перезагружен
- [ ] Логи не содержат ошибок
- [ ] Бот отвечает на сообщения в Telegram

**Если все отмечено - ты готов! 🎉**

---

**Дата создания:** 2025-12-08  
**Статус:** ✅ ПОЛНОЕ РУКОВОДСТВО  
**Версия:** 1.0
